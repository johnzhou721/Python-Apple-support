name: Catalyst

on:
  push:
    branches:
      - 3.11
      - 3.12

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Build Catalyst
      run: |
        cd macios
        make
